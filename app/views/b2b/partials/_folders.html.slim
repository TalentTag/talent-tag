.well@searches ng-controller="talent.SearchesCtrl"
  a.edit.pull-right href="javascript:;" title="Редактировать" ng-click="toggleSearchesEditMode()" ng-show="searches.length" = icon :edit
  h4 Виртуальные папки
  div ng-hide="searches.length" Сохраняйте результаты поиска в виртуальные папки. Вы всегда можете вернуться к ним позже.

  ul ng-hide="searchesEditMode"
    li ng-repeat="search in searches"
      span.glyphicon.glyphicon-folder-open
      a href="/" ng-click="loadSearch(search)" = "{{ search.name }}"

  ul.edit-mode ng-show="searchesEditMode"
    li ng-repeat="search in searches"
      a.remove-item.pull-right href="javascript:;" ng-click="deleteSearch(search)" &times;
      .input-group
        = text_field_tag :name, nil, class: "form-control", "ng-model"=>"search.name"
        small.input-group-btn = submit_tag "ok", class: "btn", "ng-click"=>"updateSearch(search)"


.well@folders ng-controller="talent.FoldersListCtrl"
  a.edit.pull-right href="javascript:;" title="Редактировать" ng-click="toggleFoldersEditMode()" ng-show="folders.length" = icon :edit
  h4 Метки
  div ng-hide="folders.length" Добавляйте метки к интересующим записям. Это поможет вам эффективно подбирать специалистов.

  ul ng-hide="foldersEditMode"
    li ng-repeat="folder in folders"
      a href="/folders/{{ folder.id }}" = "{{ folder.name }}"
      span.badge.pull-right = "{{ folder.entries.length }}"

  ul.edit-mode ng-show="foldersEditMode"
    li ng-repeat="folder in folders"
      a.remove-item.pull-right href="javascript:;" ng-click="deleteFolder(folder)" &times;
      .input-group
        = text_field_tag :name, nil, class: "form-control", "ng-model"=>"folder.name"
        small.input-group-btn = submit_tag "ok", class: "btn", "ng-click"=>"updateFolder(folder)"
